// input
const fs = require('fs');
const inputFile = 'test/input.txt';
const input = fs.readFileSync(inputFile).toString().split('\n');

// solution
let result = '';
result = input;

// output
console.log(result);
